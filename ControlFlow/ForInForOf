//Demonstrating the for in loop

let numbers = [10, 20, 30, 40];
for(let i in numbers)
{
    console.log(i + "." + " " + numbers[i]);
    //console.log(numbers[i]);
}

//Demonstrating the for of loop

numbers = [10, 20, 30, 40];
for(i of numbers)
{
    console.log(i);
}

// Find the largest number in an array using for in loop

let numberArray = [21, 32, 30, 45, 50, 60, 99, 45 ];

let largestNum = numberArray[0];

for(i in numberArray)
{
    if(numberArray[i] > largestNum)
    {
        largestNum = numberArray[i];
    }
}
console.log("The largest number is", largestNum);
